---
name: DG Daemon - Release

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - master
    paths:
      - pkg/dg-daemon/**
      - pkg/common/**
      - .github/workflows/dg-daemon-release.yml

# Queue, don't cancel: a cancelled run skips its failure-cleanup step (that only
# fires on `if: failure()`, never on `cancelled()`), which can leave a half-published
# release with no rollback. The existing "already exists" check makes a queued rerun free.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  blt:
    uses: ./.github/workflows/dg-daemon-blt.yml

  build:
    needs: blt
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: bun-linux-x64
            asset: dg-daemon-linux-x64
          - target: bun-linux-arm64
            asset: dg-daemon-linux-arm64
          - target: bun-darwin-x64
            asset: dg-daemon-macos-x64
          - target: bun-darwin-arm64
            asset: dg-daemon-macos-arm64
          - target: bun-windows-x64
            asset: dg-daemon-windows-x64.exe
          - target: bun-windows-arm64
            asset: dg-daemon-windows-arm64.exe
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.1

      - name: Install
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Build ${{ matrix.asset }}
        working-directory: pkg/dg-daemon
        run: >-
          bun build src/index.ts --compile
          --target=${{ matrix.target }}
          --outfile dist/${{ matrix.asset }}

      # Only bun-linux-x64 can execute on this ubuntu-latest runner — the one native
      # leg proves a compiled binary actually starts before any target ships.
      - name: Smoke test ${{ matrix.asset }}
        if: matrix.target == 'bun-linux-x64'
        working-directory: pkg/dg-daemon
        run: |
          if ! output="$(./dist/${{ matrix.asset }} --help)"; then
            echo "::error::${{ matrix.asset }} --help failed to run"
            exit 1
          fi
          if [ -z "$output" ]; then
            echo "::error::${{ matrix.asset }} --help produced no output"
            exit 1
          fi
          echo "$output"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset }}
          path: pkg/dg-daemon/dist/${{ matrix.asset }}
          if-no-files-found: error
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: ver
        working-directory: pkg/dg-daemon
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish dg-daemon release
        id: publish_daemon
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="daemon-v${{ steps.ver.outputs.version }}"
          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $tag already exists — nothing to publish."
            exit 0
          fi
          gh release create "$tag" \
            --repo "$GITHUB_REPOSITORY" \
            --title "DG Daemon $tag" \
            --notes "Standalone dg-daemon binaries ($tag)." \
            dist/*

      - name: Clean up the dg-daemon release on failure
        if: failure() && steps.publish_daemon.outcome == 'failure'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |-
          tag="daemon-v${{ steps.ver.outputs.version }}"
          gh release delete "$tag" --repo "$GITHUB_REPOSITORY" --yes 2>/dev/null || true
          gh api "repos/$GITHUB_REPOSITORY/git/refs/tags/$tag" --method DELETE 2>/dev/null || true
