# Slice 11 — extension-canvas-surface

Split out of the original slice 6 during step 6 synthesis (escalation E20: slice 6 was not
buildable as one slice). Its review findings live in `slice-6.md`, which reviewed both halves
before the split — see in particular the rendering-approach, pure-function-testability,
containing-block, and wheel/pointer findings, all of which apply here rather than to the page.
