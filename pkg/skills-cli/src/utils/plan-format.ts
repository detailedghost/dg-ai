// Re-exported from @dg/common — the canonical implementation lives there.
export {
	type TourScript,
	type TourStep,
	type TourMode,
	type StepAdvance,
	toPlanMarkdown,
	validate,
	extractScriptFromMarkdown,
} from "@dg/common";
