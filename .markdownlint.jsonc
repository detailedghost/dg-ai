{
  // Repo-wide markdownlint config. Line length capped at 160 (prose only —
  // code blocks and tables are exempt so they aren't forced to wrap).
  "default": true,
  "MD013": {
    "line_length": 160,
    "code_blocks": false,
    "tables": false
  }
}
